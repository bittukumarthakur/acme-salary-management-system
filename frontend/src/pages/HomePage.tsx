/**
 * Placeholder landing page.
 *
 * This is a minimal page that proves the React + Vite + TypeScript stack is
 * wired correctly. Real feature pages (employee directory, salary management,
 * dashboard) will replace/extend this in their own plans.
 */
export function HomePage() {
  return (
    <main>
      <h1>ACME Salary Management System</h1>
      <p>Frontend setup is ready.</p>
    </main>
  )
}

export default HomePage
