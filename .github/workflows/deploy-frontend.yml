name: Deploy Frontend

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: "Target GitHub environment to deploy to"
        type: environment
        default: dev

permissions:
  contents: read
  deployments: write # For automatic GitHub deployment tracking

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: frontend/yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run tests with coverage
        run: yarn test:coverage

      - name: Coverage summary
        if: always()
        run: |
          {
            echo "## 🧪 Frontend Test Coverage"
            echo ""
            if [ -f coverage/coverage-summary.json ]; then
              echo "| Metric | Covered | Total | % |"
              echo "| --- | --- | --- | --- |"
              jq -r '.total | to_entries[]
                | select(.key != "linesCovered")
                | "| \(.key) | \(.value.covered) | \(.value.total) | \(.value.pct)% |"' \
                coverage/coverage-summary.json
            else
              echo "_No coverage summary found._"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Deploy to Prisma Compute (CLI)
        id: deploy
        env:
          PRISMA_API_TOKEN: ${{ secrets.PRISMA_API_TOKEN }}
          FRONTEND_COMPUTE_SERVICE_ID: ${{ vars.FRONTEND_COMPUTE_SERVICE_ID }}
          PORT: ${{ vars.FRONTEND_PORT }}
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
        run: |
          set -o pipefail
          npx --yes @prisma/compute-cli@0.31.0 deploy \
            --service "$FRONTEND_COMPUTE_SERVICE_ID" \
            --path . \
            --entrypoint preview.mjs \
            --http-port "$PORT" \
            --env "PORT=$PORT" \
            --env "VITE_API_BASE_URL=$VITE_API_BASE_URL" \
            --env "NODE_ENV=dev" \
            --env "LOG_LEVEL=info" \
            --json | tee deploy-result.json

      - name: Print deployment result
        if: success()
        run: |
          echo "===== Deployment result ====="
          jq . deploy-result.json

          APP_NAME="$(jq -r '.data.appName // "unknown"' deploy-result.json)"

          {
            echo "## 🚀 Frontend Deployment — ${APP_NAME}"
            echo ""
            echo "**Service:** \`frontend\`  •  **App:** ${APP_NAME}"
            echo ""
            echo "| Field | Value |"
            echo "| --- | --- |"
            jq -r '.data | [
              "| App | \(.appName) |",
              "| Region | \(.region) |",
              "| Deployment ID | `\(.deploymentId)` |",
              "| Service URL | \(.appEndpointDomain) |",
              "| Version URL | \(.deploymentEndpointDomain) |",
              "| Promoted | \(.promoted) |",
              "| HTTP port | \(.resolvedConfig.portMapping.http) |",
              "| Previous deployment | `\(.previousDeploymentId // "—")` (\(.previousDeploymentAction // "none")) |"
            ] | .[]' deploy-result.json
          } >> "$GITHUB_STEP_SUMMARY"
